# Interaction System

Due to godot inheritence limitations, we need to create separate interaction systems for each type of object.
Example:
```
InteractibleStaticBody
InteractibleArea
InteractibleCharacterBody
```